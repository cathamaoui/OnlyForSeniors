import Link from "next/link"; import { ReactNode } from "react"; type Variant = "primary" | "outline"; interface CommonProps { variant?: Variant; className?: string; instruction?: string; } interface ButtonAsButtonProps extends CommonProps, Omit<React.ButtonHTMLAttributes<HTMLButtonElement>, "children"> { children?: ReactNode; href?: undefined; } interface ButtonAsLinkProps extends CommonProps { children?: ReactNode; href: string; type?: never; onClick?: never; } type ButtonProps = ButtonAsButtonProps | ButtonAsLinkProps; const variantClass: Record<Variant, string> = { primary: "btn-yp", outline: "btn-yp-outline", }; export function Button(props: ButtonProps) { const { variant = "primary", className, children, instruction, ...rest } = props as CommonProps & { children: ReactNode } & { [k: string]: unknown }; const classes = `${variantClass[variant as Variant]} ${className ?? ""}`; if ("href" in props && props.href) { return ( <span className="inline-flex flex-col items-stretch"> <Link href={props.href} className={classes}> {children as ReactNode} </Link> {instruction && <span className="instruction">{instruction}</span>} </span> ); } const buttonProps = rest as React.ButtonHTMLAttributes<HTMLButtonElement>; return ( <span className="inline-flex flex-col items-stretch"> <button className={classes} {...buttonProps}> {children as ReactNode} </button> {instruction && <span className="instruction">{instruction}</span>} </span> ); } 