import Image from "next/image"; import Link from "next/link"; import { MapPin, Star, BadgeCheck } from "lucide-react"; import { Business, getCategoryBySlug, getSubcategory } from "@/lib/businesses"; import { SaveButton } from "./SaveButton"; type Props = { business: Business; }; export function BusinessCard({ business }: Props) { const sub = getSubcategory(business.subcategorySlug); return ( <Link href={`/businesses/${business.id}`} className="group flex flex-col bg-white border-2 border-black rounded-lg overflow-hidden transition-shadow hover:shadow-lg" > {/* Image */} <div className="relative aspect-[4/3] bg-stone-100 border-b-2 border-black"> <Image src={business.image} alt={business.name} fill sizes="(min-width: 1024px) 25vw, (min-width: 640px) 33vw, 50vw" className="object-cover" /> {business.isFeatured && ( <span className="absolute top-2 left-2 bg-white text-black text-base font-bold px-2 py-1 rounded border border-black"> Featured </span> )} <SaveButton /> </div> {/* Content */} <div className="flex-1 p-4 flex flex-col gap-2"> <div className="flex items-start justify-between gap-2"> <h3 className="font-display text-lg leading-normal group-hover:underline line-clamp-2"> {business.name} </h3> {business.isVerified && ( <BadgeCheck className="w-5 h-5 text-emerald-700 flex-shrink-0" aria-label="Verified" /> )} </div> {business.tagline && ( <p className="text-base text-stone-700 line-clamp-2">{business.tagline}</p> )} <div className="flex items-center gap-1 text-base text-stone-800 flex-wrap"> <MapPin className="w-3.5 h-3.5" /> <span>{business.city}, {business.province}</span> {sub && ( <> <span>·</span> <span>{sub.subcategory.name}</span> </> )} </div> {business.rating !== undefined && ( <div className="flex items-center gap-1 text-base"> <Star className="w-4 h-4 fill-stone-900 stroke-black" /> <span className="font-semibold">{business.rating.toFixed(1)}</span> {business.reviewCount !== undefined && ( <span className="text-stone-800">({business.reviewCount})</span> )} </div> )} {business.priceRange && ( <div className="text-base font-display font-bold text-black"> {business.priceRange} </div> )} {business.tags && business.tags.length > 0 && ( <div className="flex flex-wrap gap-1 mt-1"> {business.tags.slice(0, 3).map((tag) => ( <span key={tag} className="text-base px-2 py-0.5 bg-stone-100 border border-stone-500 rounded" > {tag} </span> ))} </div> )} </div> </Link> ); } 